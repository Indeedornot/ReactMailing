Client:
    ☐ Add Error handling for error responses from fetching emails
    ☐ Add signal for when emails no emails are found
    ☐ Add Error message for invalid imap data in form
    ☐ Label for LTS checkmark and add tooltip

Server:

Shared:
    ☐ Rethink implementation of EmailSortArgs on Shared rather than on Client
    due to being coupled with implementation of sort helper on Client